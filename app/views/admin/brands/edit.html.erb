<div class="form-block">
  <h3> Редактировать бренд</h3>
  <%= form_for [:admin, @brand] do |f| %>

      <div class="form-row">
        <%= f.label :title, 'Название' %>
        <%= f.text_field :title, placeholder: 'Название' %>
      </div>

      <div class="form-row">
        <input type="submit" class="submit_btn" value="Обновить" >
      </div>
  <% end %>
</div>